export interface RtcMessageCategory {
  /**
   * stream-published
   */
  STREAM_PUBLISHED: string;
  /**
   * stream-added
   */
  STREAM_ADD: string,
  /**
   * stream-subscribed
   */
  STREAM_SUBSCRIBED: string,
  /**
   * stream-removed
   */
  STREAM_REMOVED: string,
  /**
   * peer-leave
   */
  PEER_LEAVE: string,
}
